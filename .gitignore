# Python
**/__pycache__/      # Ignore __pycache__ directories anywhere
**/*.py[cod]         # Ignore compiled Python files anywhere
**/*.so              # Ignore shared object files (compiled extensions) anywhere
**/*.egg             # Ignore .egg files anywhere
**/*.egg-info/       # Ignore .egg-info directories anywhere
**/*.pyd             # Ignore .pyd files (Windows DLLs for Python) anywhere
.Python              # Ignore .Python directory (sometimes created by venv)
build/               # Ignore build directory at the root
dist/                # Ignore dist directory at the root

# Environment
.venv/               # Your main Python 3.11 virtual environment
.env                 # Your local environment variables file (CRUCIAL for security)

# Pytest
.pytest_cache/       # Pytest cache directory

# Whisper
whisper_cache/

# VSCode and other IDEs
.vscode/             # VSCode configuration
.idea/               # IntelliJ/PyCharm configuration
*.sublime-project    # Sublime Text project files
*.sublime-workspace  # Sublime Text workspace files

# Data and outputs (these are mounted as volumes in Docker, not part of image)
data/                # Your raw and processed data
out/                 # Any other general output directory
logs/                # Your log files
app/logs/            # App log files
**/*.log             # Any .log file

# Mac/Linux OS specific files
.DS_Store            # macOS directory service store
Thumbs.db            # Windows thumbnail cache (can appear on Linux if mounted Windows shares)

# Docker specific ignores (files generated by Docker locally)
*.container          # Docker container related files
*.pid                # Process ID files
# *.log              # Removed, as logs/ is already ignored

# Miscellaneous temporary files
*.bak                # Backup files
*.swp                # Vim swap files
*.tmp                # Generic temporary files
*.old                # Old versions of files
